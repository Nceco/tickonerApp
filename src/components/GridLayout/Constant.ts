/*gridItem layouts配置*/
//最小w
export const GRID_MIN_HEIGHT = 6
//最大h
export const GRID_MAX_WIDTH = 12
//最小h
export const GRID_MIN_WIDTH = 4
//最大h
export const GRID_MAX_HEIGHT = 16

export const DEFAULT_LIMIT_PARAMS: {
  minH: number,
  minW: number,
  maxH: number,
  maxW: number
} = {
  minH: GRID_MIN_HEIGHT,
  minW: GRID_MIN_WIDTH,
  maxH: GRID_MAX_HEIGHT,
  maxW: GRID_MAX_WIDTH
}
